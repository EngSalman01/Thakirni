{
  "name": "thakirni",
  "version": "1.0.0",
  "private": true,
  "description": "Thakirni — AI-powered personal assistant & second brain",
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\"",
    "build": "pnpm --filter backend build && pnpm --filter frontend build",
    "start": "concurrently \"pnpm --filter frontend start\" \"pnpm --filter backend start\"",
    "lint": "pnpm --filter frontend lint",
    "type-check": "pnpm --filter frontend exec tsc --noEmit && pnpm --filter backend exec tsc --noEmit"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.28.2",
  "dependencies": {
    "@sentry/nextjs": "^10"
  }
}